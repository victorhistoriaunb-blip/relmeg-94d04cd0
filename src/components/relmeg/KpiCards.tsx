import { Database, Columns3, Filter, BarChart3 } from "lucide-react";
import type { DataRecord, DataColumn } from "@/lib/relmeg/types";
export function KpiCards({ data, columns }: { data: DataRecord[]; columns: DataColumn[] }) { const filled=data.reduce((n,r)=>n+columns.filter(c=>r.data[c.key]!==null&&r.data[c.key]!=="").length,0); const cards=[{title:"Registros",value:String(data.length),hint:"na seleção atual",Icon:Database},{title:"Colunas",value:String(columns.length),hint:"detectadas na base",Icon:Columns3},{title:"Dados preenchidos",value:String(filled),hint:"células com conteúdo",Icon:Filter},{title:"Campos numéricos",value:String(columns.filter(c=>c.type==="number").length),hint:"disponíveis para cálculos",Icon:BarChart3}]; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(({title,value,hint,Icon})=><div key={title} className="panel rounded-xl p-4"><div className="flex justify-between gap-3"><p className="text-[11px] uppercase text-muted-foreground">{title}</p><Icon className="h-4 w-4 text-primary"/></div><p className="mt-3 font-display text-2xl font-semibold">{value}</p><p className="mt-1 text-xs text-muted-foreground">{hint}</p></div>)}</div> }
